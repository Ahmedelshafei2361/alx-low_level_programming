#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char as;
	char sa;

	for (as = 'a'; as <= 'z'; as++)
	{
		putchar(as);
	}
	putchar('\n');

	return (0);
}
